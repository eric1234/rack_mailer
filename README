= Description

A rack end point for a contact form. Will put all the params in an
e-mail sent to a pre-configured address.

= Usage

== Rack

  Rack::Builder.app do
    map '/mail_to' do
      run Rack::Mailer.new('joe@example.com')
    end
  end

== Rails

  match '/mail_to' => RackMailer.new('joe@example.com')

Will send any forms posted to /mail_to to joe@example.com.

= Todo

Features to add/verify:

* Support nested params
* Support file uploads
* Add spam protection
* Make sure the above Rails code actually works
* Add the ability to specify required fields and/or other validation
